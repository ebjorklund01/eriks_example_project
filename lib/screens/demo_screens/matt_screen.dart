import 'package:flutter/material.dart';

class MattScreen extends StatelessWidget {
  const MattScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Matt Screen')),
      body: const Text('Matt Screen'),
    );
  }
}
