import 'package:flutter/material.dart';

class LucasScreen extends StatelessWidget {
  const LucasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Lucas Screen')),
      body: const Text('Lucas Screen'),
    );
  }
}
