import 'package:flutter/material.dart';

class AlexeyScreen extends StatelessWidget {
  const AlexeyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Alexey Screen')),
      body: const Text('Alexey Screen'),
    );
  }
}
