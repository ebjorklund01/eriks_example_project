import 'package:flutter/material.dart';

class ErikScreen extends StatelessWidget {
  const ErikScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Erik Screen')),
      body: const Text('Erik Screen'),
    );
  }
}
