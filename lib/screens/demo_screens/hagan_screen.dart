import 'package:flutter/material.dart';

class HaganScreen extends StatelessWidget {
  const HaganScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hagan Screen')),
      body: const Text('Hagan Screen'),
    );
  }
}
