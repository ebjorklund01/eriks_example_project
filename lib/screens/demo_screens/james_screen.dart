import 'package:flutter/material.dart';

class JamesScreen extends StatelessWidget {
  const JamesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('James Screen')),
      body: const Text('James Screen'),
    );
  }
}
