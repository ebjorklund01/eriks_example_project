import 'package:flutter/material.dart';

class OksanaScreen extends StatelessWidget {
  const OksanaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Oksana Screen')),
      body: const Text('Oksana Screen'),
    );
  }
}
